# Bóveda de conocimiento / documentación de trabajo interna (contiene
# credenciales de desarrollo y el historial de auditoría de seguridad de cada
# proyecto): no publicar
boveda/
.claude/

# Variables locales del compose (hay un .env.example como plantilla)
compose/inafinder/.env
